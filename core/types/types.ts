const number = 5 //5.2 // -5

const string = 'Hello' // `hello`

const bollean = true // false

const object = { name: 'Akash' }

const array = ['1', '2', '3']

const tuple = [1, 'a'] //fix array length

//const enum1 = // enum {new,old}

//const anything: any  //*

//const union = number | string | bollean   // different types of value

const literal = string //exact value

type combinable = number // custom  or Aliases types

let userInput: unknown // unknown type

const u: undefined = undefined

const n: null = null
